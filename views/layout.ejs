<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniEvents | University Event Management System</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <!-- Alpine.js for State Management (UI Only) -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <!-- HTMX for server interactions -->
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <style>
        [x-cloak] {
            display: none !important;
        }

        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {
            background: #f1f1f1;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: #888;
            border-radius: 10px;
        }

        .event-card:hover {
            transform: translateY(-4px);
            transition: all 0.3s ease;
        }
    </style>
</head>

<body class="bg-slate-50 text-slate-900 font-sans">

    <!-- Navigation -->
    <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200"
        x-data="{ mobileMenu: false }">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <!-- Logo -->
                <a href="/" class="flex items-center space-x-2 cursor-pointer" hx-boost="true">
                    <div class="bg-indigo-600 p-2 rounded-lg">
                        <i data-lucide="graduation-cap" class="text-white w-6 h-6"></i>
                    </div>
                    <span
                        class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-violet-600">UniEvents</span>
                </a>

                <!-- Desktop Nav -->
                <div class="hidden md:flex items-center space-x-8">
                    <a href="/"
                        class="<%= view === 'home' ? 'text-indigo-600 border-b-2 border-indigo-600' : 'text-slate-600 hover:text-indigo-600' %> font-medium py-5 transition-all"
                        hx-boost="true">Discover</a>
                    <a href="/my-events"
                        class="<%= view === 'my-events' ? 'text-indigo-600 border-b-2 border-indigo-600' : 'text-slate-600 hover:text-indigo-600' %> font-medium py-5 transition-all"
                        hx-boost="true">My
                        Tickets</a>
                    <a href="/dashboard"
                        class="<%= view === 'dashboard' ? 'text-indigo-600 border-b-2 border-indigo-600' : 'text-slate-600 hover:text-indigo-600' %> font-medium py-5 transition-all"
                        hx-boost="true">Dashboard</a>

                    <div class="h-6 w-px bg-slate-200"></div>

                    <a href="/settings"
                        class="<%= view === 'settings' ? 'text-indigo-600' : 'text-slate-600 hover:text-indigo-600' %> p-2 rounded-full hover:bg-slate-100 transition-colors"
                        title="Settings" hx-boost="true">
                        <i data-lucide="settings" class="w-5 h-5"></i>
                    </a>

                    <a href="/create"
                        class="<%= view === 'create' ? 'bg-indigo-700' : 'bg-indigo-600' %> text-white px-5 py-2.5 rounded-full font-bold hover:bg-indigo-700 transition-shadow shadow-lg shadow-indigo-100 flex items-center gap-2"
                        hx-boost="true">
                        <i data-lucide="plus" class="w-4 h-4"></i>
                        Post Event
                    </a>

                    <div class="relative" x-data="{ open: false }">
                        <button @click="open = !open"
                            class="flex items-center gap-2 bg-slate-100 p-1 pr-3 rounded-full hover:bg-slate-200 transition-colors">
                            <img src="https://i.pravatar.cc/100?u=<%= currentUser.role %>"
                                class="w-8 h-8 rounded-full border border-white">
                            <span class="text-xs font-bold capitalize">
                                <%= currentUser.role %>
                            </span>
                        </button>
                        <div x-show="open" @click.away="open = false" x-cloak
                            class="absolute right-0 mt-2 w-48 bg-white rounded-xl shadow-xl border border-slate-100 p-2 z-50">
                            <button hx-post="/htmx/role" hx-vals='{"role": "student"}'
                                class="w-full text-left px-4 py-2 hover:bg-indigo-50 rounded-lg text-sm transition-colors flex items-center justify-between">
                                Student Mode
                                <% if (currentUser.role==='student' ) { %><i data-lucide="check"
                                        class="w-4 h-4 text-indigo-600"></i>
                                    <% } %>
                            </button>
                            <button hx-post="/htmx/role" hx-vals='{"role": "organizer"}'
                                class="w-full text-left px-4 py-2 hover:bg-indigo-50 rounded-lg text-sm transition-colors flex items-center justify-between">
                                Organizer Mode
                                <% if (currentUser.role==='organizer' ) { %><i data-lucide="check"
                                        class="w-4 h-4 text-indigo-600"></i>
                                    <% } %>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Mobile Toggle -->
                <div class="flex items-center md:hidden gap-4">
                    <a href="/settings" class="text-slate-600 p-2"><i data-lucide="settings" class="w-5 h-5"></i></a>
                    <button @click="mobileMenu = !mobileMenu"
                        class="text-slate-600 p-2 hover:bg-slate-100 rounded-lg transition-colors">
                        <i x-show="!mobileMenu" data-lucide="menu"></i>
                        <i x-show="mobileMenu" data-lucide="x"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div x-show="mobileMenu" x-transition:enter="transition ease-out duration-200"
            x-transition:enter-start="opacity-0 -translate-y-4" x-transition:enter-end="opacity-100 translate-y-0"
            x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100 translate-y-0"
            x-transition:leave-end="opacity-0 -translate-y-4" x-cloak
            class="md:hidden bg-white border-b border-slate-200 overflow-hidden">
            <div class="px-4 pt-2 pb-6 space-y-1">
                <a href="/"
                    class="flex items-center gap-3 px-4 py-3 rounded-xl font-medium <%= view === 'home' ? 'bg-indigo-50 text-indigo-600' : 'text-slate-600' %>"
                    hx-boost="true">
                    <i data-lucide="compass" class="w-5 h-5"></i> Discover
                </a>
                <a href="/my-events"
                    class="flex items-center gap-3 px-4 py-3 rounded-xl font-medium <%= view === 'my-events' ? 'bg-indigo-50 text-indigo-600' : 'text-slate-600' %>"
                    hx-boost="true">
                    <i data-lucide="ticket" class="w-5 h-5"></i> My Tickets
                </a>
                <a href="/dashboard"
                    class="flex items-center gap-3 px-4 py-3 rounded-xl font-medium <%= view === 'dashboard' ? 'bg-indigo-50 text-indigo-600' : 'text-slate-600' %>"
                    hx-boost="true">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i> Dashboard
                </a>
                <div class="pt-4 mt-4 border-t border-slate-100">
                    <a href="/create"
                        class="flex items-center justify-center gap-2 w-full bg-indigo-600 text-white px-5 py-3 rounded-xl font-bold shadow-lg shadow-indigo-100"
                        hx-boost="true">
                        <i data-lucide="plus" class="w-4 h-4"></i> Host Event
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <main id="main-content" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <%- body %>
    </main>

    <!-- Notification Container -->
    <div id="toast-container" class="fixed bottom-8 left-1/2 -translate-x-1/2 z-[200] flex flex-col gap-2"></div>

    <script>
        document.body.addEventListener('htmx:afterOnLoad', function (evt) {
            lucide.createIcons();
        });
        window.addEventListener('load', function () {
            lucide.createIcons();
        });
    </script>
</body>

</html>